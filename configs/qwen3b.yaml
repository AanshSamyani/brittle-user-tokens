# Override layered on top of default.yaml. Pass BOTH, e.g.:
#   python scripts/04_generate_eval.py --config configs/default.yaml configs/qwen3b.yaml --base --seed 0
# Reuses data/ + data/transforms/ (model-independent); namespaces runs/results so 7B output isn't clobbered.
model:
  name: Qwen/Qwen2.5-3B-Instruct
paths:
  runs_dir: runs_qwen3b
  results_dir: results_qwen3b
