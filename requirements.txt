# --- core ---
openai>=1.40
pyyaml>=6.0
numpy>=1.26
pandas>=2.0
scipy>=1.11
tqdm>=4.66

# --- training / eval (GPU box) ---
# install a torch build matching your CUDA first, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.3
transformers>=4.44
datasets>=2.20
peft>=0.12
trl>=0.10
accelerate>=0.33

# --- optional ---
# vllm>=0.5            # fast eval generation (recommended on the GPU box)
# sentence-transformers>=3.0   # embedding-based content-preservation check
# matplotlib>=3.8      # heatmaps in 06_analyze
# seaborn>=0.13

# --- dev ---
pytest>=8.0
